[package]
name = "musubi-field-contracts"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Detached, read-only sidecar contracts for extensible Musubi observations, annotations, integration cases, and SIM_SELF_ATTEST receipts."

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = { version = "0.10", default-features = false }
